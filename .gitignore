node_modules/
# dist/ is intentionally committed so `pi install git:...` works without
# needing TypeScript at install time (pi runs `npm install --omit=dev`,
# which skips devDependencies like typescript).
*.log
.DS_Store
.env
.env.local
test/live-cert-test.mjs
test/debug-env.mjs